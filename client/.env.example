# Backend API URL for production (set when frontend is hosted separately on Vercel/Netlify)
# If frontend is hosted together on Render, leave empty
VITE_API_URL=https://tirumala-clg-attendence.onrender.com
